/**
 * Material Design 3 — Context Menu (right-click triggered)
 *
 * TODO: Implement ContextMenu component once @radix-ui/react-context-menu is installed.
 * The API shape mirrors DropdownMenu:
 *   - ContextMenu — root (wraps trigger element + menu)
 *   - ContextMenuItem — same styling as MenuItem
 *   - ContextMenuDivider — same as MenuDivider
 *   - ContextMenuHeader — same as MenuHeader
 *
 * Install with: npm install @radix-ui/react-context-menu
 */

export {};
